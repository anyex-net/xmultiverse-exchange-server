package com.anyex.apps.utils;

import com.anyex.apps.consts.CacheConst;
import com.anyex.apps.consts.GlobalConst;

public class RedisCache
{
//    // 账户信息KEY: platscan_fundCurrent_[acctid]
//    private static final String      keyPrefix        = new StringBuffer(CacheConst.REDIS_PLATSCAN_PREFIX).append(GlobalConst.SEPARATOR).append(GlobalConst.OP_FUND_ASSET)
//            .append(GlobalConst.SEPARATOR).toString();
//
//    public static void clearAccountAssetCache(Long accountId, Long exchangePairVCoin, Long exchangePairMoney)
//    {
//        String key = new StringBuilder(keyPrefix).append(accountId).append(GlobalConst.SEPARATOR).append(exchangePairMoney).toString(); // changescan_fundCurrent_[acctid]
//        RedisUtils.del(key);
//    }
}
